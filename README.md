# heartbeat
SUPER basic code, lets you monitor the status of your VPS.

### To use...
Simply clone this git repo, then in your env add:
```
HEARTBEAT_URL=
INTERVAL=5m
```
Heartbeat URL is just the URL for it to call every x minutes.
